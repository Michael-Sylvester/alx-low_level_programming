#include "main.h"
/**
 * set_string - A function that sets the value of a pointer to a char.
 * @s :destination memory pointer
 * @to :pointer to string that we want to replace
 *
 * Return: nothing
 */
void set_string(char **s, char *to)
{
*s = to;
}
